# nginx (Phase 5)

Placeholder for the production reverse proxy: HTTPS/WSS termination in front
of the dashboard server, static asset caching, and CMU hostname config.
Not used in Phases 1–2 (the FastAPI server serves the built frontend itself).
