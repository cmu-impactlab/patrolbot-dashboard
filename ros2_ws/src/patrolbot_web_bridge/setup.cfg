[develop]
script_dir=$base/lib/patrolbot_web_bridge
[install]
install_scripts=$base/lib/patrolbot_web_bridge

# See server/pyproject.toml: a hanging suite must fail, not stall `make test`.
[tool:pytest]
timeout = 60
timeout_method = thread
