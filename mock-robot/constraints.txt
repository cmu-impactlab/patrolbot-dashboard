# Resolved runtime dependency set for the mock robot image.
#
# Same contract as server/constraints.txt: pyproject.toml says what is needed,
# this says what the image ships. The mock is what demos and local development
# run against, so it drifting quietly is how a demo breaks for reasons nobody
# can reproduce.
#
# Regenerate (from the repository root):
#
#   docker run --rm -v "$PWD:/repo:ro" python:3.12-slim bash -c '
#     cp -r /repo/mock-robot /tmp/m && rm -rf /tmp/m/*.egg-info
#     pip install --quiet --no-cache-dir --root-user-action=ignore /tmp/m
#     pip freeze --exclude-editable | grep -v "^patrolbot-mock-robot" \
#       | LC_ALL=C sort'
#
# scripts/check-constraints.sh checks this alongside the server's.
psutil==7.2.2
websockets==17.0.1
